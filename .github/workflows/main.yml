name: Getting started

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "Building..."
  
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing...."
